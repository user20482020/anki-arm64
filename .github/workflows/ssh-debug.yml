name: CI
on: [workflow_dispatch]
jobs:
  build:
    runs-on: macos-10.15
    steps:    
    - uses: actions/checkout@v2
    
    - name: Run vagrant up
      run: vagrant up
    
    - name: Setup upterm session
      uses: lhotari/action-upterm@v1
